# 🇬🇪 RS InfoHub — RAG აგენტი

საგადასახადო და საბაჟო კითხვებზე პასუხის გამცემი **RAG (Retrieval-Augmented Generation)** აგენტი.

## როგორ მუშაობს

```
docs/ საქაღალდის ფაილები
        ↓
   chunk-ებად დაყოფა
        ↓
  keyword retrieval
        ↓
  Groq Llama 3 LLM
        ↓
    პასუხი + წყარო
```

## პროექტის სტრუქტურა

```
rs-agent/
├── app.py              ← მთავარი კოდი
├── requirements.txt    ← პაკეტები
├── README.md           ← ეს ფაილი
└── docs/               ← შენი დოკუმენტები (.txt ან .pdf)
    ├── dagvasaxadi_kodeksi.txt
    ├── mcire_biznesi.txt
    └── ...
```

## ახალი დოკუმენტის დამატება

1. `.txt` ან `.pdf` ფაილი ჩადე `docs/` საქაღალდეში
2. GitHub-ზე ატვირთე
3. Streamlit ავტომატურად ჩართავს — კოდი არ იცვლება

## ლოკალურად გაშვება

```bash
# 1. კლონირება
git clone https://github.com/შენი-username/rs-agent.git
cd rs-agent

# 2. პაკეტების ინსტალაცია
pip install -r requirements.txt

# 3. API Key-ის დაყენება
mkdir .streamlit
echo 'GROQ_API_KEY = "gsk_შენი_გასაღები"' > .streamlit/secrets.toml

# 4. გაშვება
streamlit run app.py
```

## Streamlit Cloud-ზე Deploy

1. GitHub-ზე ატვირთე პროექტი
2. გადადი [share.streamlit.io](https://share.streamlit.io)
3. **New app** → აირჩიე რეპო → Main file: `app.py`
4. **Advanced settings → Secrets**:
```toml
GROQ_API_KEY = "gsk_შენი_გასაღები"
```
5. **Deploy!**

## გამოყენებული ტექნოლოგიები

| ტექნოლოგია | დანიშნულება |
|---|---|
| Streamlit | UI და hosting |
| LangChain | RAG framework |
| Groq + Llama 3.3 70B | LLM |
| Keyword Retrieval | ქართული ტექსტის ძიება |
| pypdf | PDF-ების წაკითხვა |

## წყარო

[RS InfoHub](https://infohub.rs.ge/ka) — საგადასახადო და საბაჟო ადმინისტრირების დოკუმენტები
